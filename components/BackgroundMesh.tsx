/**
 * BackgroundMesh — intentionally empty. The page sits on pure white,
 * unornamented. Kept as a component so layouts that import it don't break.
 */
export function BackgroundMesh() {
  return null;
}
